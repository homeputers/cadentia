package com.cadentia.reng.scoring;

public enum HardFilterReasonCode {
    EXCLUDED_BY_USER,
    MISSING_PROVENANCE,
    MISSING_APPROVAL_SUMMARY,
    FAILED_APPROVAL_GATE,
    UNSUPPORTED_LANGUAGE,
    MISSING_KEY,
    MISSING_TEMPO
}
